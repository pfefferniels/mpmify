ready: False
blockers: [
 "B1 (DESTRUCTIVE, verified by simulation) \u2014 dsl.py has no frozen v2 vocab size. Appending the v3 tokens `U F I X A L W` makes len(VOCAB)=31; train.py:75 computes `\"vocab_size\": len(VOCAB) if V2 else V1_VOCAB_SIZE`, so the resume guard at train.py:88 (`all(prev_cfg.get(k, MODEL_CFG[k]) == MODEL_CFG[k] ...)`) fails against runs/v2/ckpt.pt (config vocab_size=24, epoch 11) and train.py SILENTLY starts from epoch 0 and overwrites the checkpoint at the end of epoch 0. A training IS running right now: PID 46625 `python3 train.py 24 v2 v2`, resumed at epoch 12, 1.16 s/step, ~1.48 GB RSS. Simulated: len(VOCAB)=24 -> guard True; 31 -> False; 33 -> False. FIX BEFORE TOUCHING dsl.py: add `V2_VOCAB_SIZE = 24` next to `V1_VOCAB_SIZE = 19`, make train.py pick V1/V2/V3 vocab size by an explicit version flag (never bare len(VOCAB)), and make the config-mismatch branch abort with an error instead of silently restarting.",
 "B2 (schema collision, verified in data/) \u2014 two incompatible `rubato` row formats exist on disk. Team A / SampleAndRender (the TRAINING path): `[date, frameLength, intensity, lateStart, earlyEnd, loop01]`, 6 fields, with an explicit neutral TERMINATOR row (intensity == 1.0) after every span. Team B / RubatoProbe (analysis only): `[date, frameLength, intensity, endDate]`, 4 fields, no terminator row (data/pilot_rubato.jsonl, data/pilot_rubato_multitempo.jsonl; the latter also carries extra `perf`/`straddle` keys and deliberately violates R8 with NaN renders). perf_chain.PerfChain / rubato_math.RubatoTimeline accept ONLY the 6-field form (RUB_DATE..RUB_LOOP = indices 0..5). Any v3 dsl/dataset/preprocess/evaluate code must consume the 6-field form and must never ingest pilot_rubato*.jsonl.",
 "B3 (metrics silently wrong on v3) \u2014 evaluate.py renders with tempo_math only. Measured on 40 pilot_v3 pieces: render_rmse fed the GROUND-TRUTH tempo map gives median 84.7 ms / max 620.2 ms on the rubato-carrying pieces (floor should be 0.0; it is 0.000 on the rubato-free ones), and velocity_render_rmse fed the GROUND-TRUTH dynamics map gives median 5.55 / max 8.89 velocity units because articulation absoluteVelocityChange is never applied. v3 render/velocity metrics MUST route through perf_chain.PerfChain(tempo, dynamics, articulation, rubato).render([(n[0], n[1]) for n in rec['notes']]) \u2014 I re-verified it at max|diff| 0.0 ms and 0.0 velocity over 15,520 notes of the three canonical pilots, at ~2,820 notes/s.",
 "B4 (eval-loop crash / NaN poisoning from model output) \u2014 no sanitizer exists for predicted rubato/articulation, and PerfChain is strict. Reproduced: unsorted rubato or articulation -> ValueError from _require_sorted; frameLength == 0 -> `ValueError: math domain error` at rubato_math.py:459 (`math.fmod(date-start, 0)`), where Java `x % 0.0` returns NaN (a genuine port divergence, canonical-form-unreachable); intensity < 0 -> 4 notes NaN ms_on and 3 notes with `ms_off is None` (silent, then TypeError downstream); an R8 violation (tempo transition boundary strictly inside a rubato frame, intensity 2.0) -> 3/32 notes NaN ms. A single NaN propagates through statistics.median at train.py:135 and poisons the whole epoch's metric row. Need dsl._sanitize_rubato/_sanitize_artic (sort, dedupe by date, frameLength in {720,1440,2880}, intensity > 0 with the R3 deadband, force a terminator at span end, split/drop spans straddling a tempo date per R8) plus a math.isfinite / None guard in the v3 metric.",
 "B5 (render-back loop not closed for v3) \u2014 dsl_to_mpm.maps_to_mpm(tempo_map, dyn_map, ppq, name, beat_length, parts, allow_no_tempo) has no articulationMap/rubatoMap vocabulary, so DSL -> .mpm -> meico RenderMpm cannot validate v3 (Team C already measured validate_export.py on a v3 pilot as MISMATCH: 304.26 ms onset, 24.9 velocity, 0/3 byte-identical). Either extend the exporter with <articulation>/<rubato> elements plus a matching validation pass, or explicitly scope v3 proof-of-exactness to validate_v3.py (Python-side, bit-exact) and defer the XML round-trip. Decide before claiming v3 exactness."
]

STATE VERIFIED ON DISK: all deliverables exist and import cleanly (dsl, dataset, evaluate, model, tempo_math, dynamics_math, rubato_math, perf_chain, validate_v3, dsl_to_mpm, vienna_adapter all OK); java/out/*.class are current (SampleAndRender.class 19:54 vs .java 19:53; RenderMpm.class 19:37 vs .java 19:37). PerfChain reproduces the canonical v3 pilots at 0.0 ms / 0.0 velocity over 15,520 notes. Rubato canonical form re-audited over 4 pilots: 297 spans, 0 unpaired, terminator frameLength always == span frameLength, lateStart=0 / earlyEnd=1 / loop=1 on 297/297 — so CANONICAL.md's "R2 vs the shipped sampler — unresolved conflict" block and the R6 "sampler writes frameLength=720, loop=false" parenthetical are STALE. Articulation: 1,751 instructions, dates strictly on the 180-tick grid, velChange integer, relDur in [0.40,1.15] with the deadband clean.

ORDERED PLAN

1. FREEZE VOCAB (first, before any dsl.py edit). Add `V2_VOCAB_SIZE = 24` to dsl.py; change train.py:75 to select V1_VOCAB_SIZE / V2_VOCAB_SIZE / len(VOCAB) by an explicit version flag; make the resume-guard mismatch at train.py:88 raise instead of silently restarting. Without this, editing dsl.py destroys the live v2 run (B1). The running process has already imported dsl.py, so the edit itself is safe — only a restart is fatal.

2. EXTEND dsl.py ADDITIVELY. Append `U F I X A L W` AFTER the existing 24 entries (v1/v2 ids stay stable; mdl.BITS_PER_TOKEN shifts 4.585 -> 4.954, expected). Add `encode_piece_v3` / `decode_piece_v3` + `_sanitize_rubato` / `_sanitize_artic` as NEW functions; leave encode_piece/decode_piece/decode_tokens byte-identical (vienna-adapter is live on them, and ml/ is entirely untracked in git — `?? ml/` — so there is no VCS protection against concurrent edits).
   Grammar per CANONICAL.md §5: `rubato := 'U' date 'F' frameBeats 'I' intensity 'X' endDate`, `articulation := 'A' date 'L' relativeDuration 'W' velocityChange`.
   Two conventions the encoder must implement: (a) rubato rows are PAIRED — a span is a row with intensity != 1.0, the following row (intensity == 1.0) is its terminator and supplies `X endDate`; `loop` no longer distinguishes them (both are 1 after Team A's R6 fix). The decoder must re-emit BOTH rows, terminator inheriting frameLength with lateStart=0, earlyEnd=1, loop=1. (b) articulation dates are NOT beat-integer — they sit on quarter-beat fractions {0, .25, .5, .75}; use date/ppq unrounded (the existing `%g` formatter gives clean '5.25'), NOT the `round(date/ppq)` used for tempo/dynamics.

3. RAISE THE LENGTH BUDGET. I counted v3 targets under the §5 grammar over pilot_v3: median 161 tokens, p90 263, p99 313, max 313 — against preprocess.py `max_tgt = 320` and train.py `MAX_DECODE = 320`. Only 7 tokens of headroom; LOG.md's planned v3 domain randomization (bpm [25,240], 90-tick grid, more tempo instructions) will push past it and preprocess.py will SILENTLY skip pieces. Set max_tgt/MAX_DECODE to ~448 and log the skip count. Greedy decode is O(MAX_DECODE) per eval piece, so eval wall time rises ~1.4x.

4. EXTEND preprocess.py. Add a `--v3` flag: use encode_piece_v3, keep `with_velocity=True` (notes are uniformly 6-wide in every v3 pilot, so the existing `torch.tensor(ns, dtype=torch.float64)` stays rectangular — verified), and store `articulation` + `rubato` alongside `tempo`/`dynamics` under `--eval` so the evaluator has the GT maps.

5. EXTEND evaluate.py. Add `evaluate_piece_v3` that builds one PerfChain per candidate and compares ms_on / velocity against rec['notes'][3] / [5], with an isfinite/None guard (B3, B4). Baseline = constant tempo, no rubato/articulation. Cost check: 100 pieces ~= 5.1k notes ~= 1.8 s per render pass, ~4 s per eval — negligible against ~370 s/epoch. Do NOT put PerfChain in a loop over the training set (20k pieces ~= 6 min at 2,820 notes/s).

6. FIX THE TWO PORT DEFECTS IN tempo_math.py / dynamics_math.py (both still unfixed on disk, verified): lines 71/81 and line 68 use `float("inf")` where meico uses Double.MAX_VALUE — dynamics_math._t_for_date then never terminates on a dangling final dynamics transition (Team C hung 3.5 min at 96% CPU; currently unreachable because dsl._sanitize_dyn forces the last instruction constant, but any v3 code feeding a raw map hangs); and tempo_math.py:21-22 use platform `math.pow`/`math.log`, which are 1 ulp off Java for 1,701/103,562 values of val_v2.jsonl (max 7.3e-12 ms). Import `java_pow`/`java_log` and `JAVA_DOUBLE_MAX` from rubato_math. Then correct LOG.md: the v1/v2 "0.000000000 ms" is a %.9f artefact, not bit-exactness.

7. REFACTOR THE fdlibm PORT out of rubato_math.py into ml/python/java_libm.py (java_pow, java_log, _scalbn, _recip, JAVA_DOUBLE_MAX + the 139-vector selftest) and import it from rubato_math, perf_chain, tempo_math, dsl_to_mpm. Pure move, but it touches three teams' files — do it in one pass while nothing else is mid-edit. Bit-exactness is tied to the generating JVM (Math.pow == StrictMath.pow verified 0/2e6 on Zulu 17.0.1 aarch64); re-run that check if generation moves.

8. RESOLVE THREE SAMPLER-vs-SPEC DECISIONS BEFORE GENERATING v3 (these get baked into the labels):
   (a) R3 deadband — CANONICAL.md now makes it frame-dependent and normative ([0.91,1.10] at frameLength 720, [0.89,1.12] if bpm widens to 240); SampleAndRender.java:422-423 still excludes only [0.95,1.05] for every frame. 16/297 spans in the current pilots (5.4%) sit in the normative-but-unobservable band, i.e. 13-token labels whose entire effect is below the 5 ms floor.
   (b) T2 (depth < 0.32 requires a >= 8-beat segment) is NOT implemented: 21 violations / 300 canonical pieces. It lives in sampleTempoMap, shared with v1/v2 — implementing it desyncs the already-generated train_v2/val_v2/test_v2. Choose: implement + regenerate v2, or relax T2 in CANONICAL.md.
   (c) frameLength distribution is structurally skewed 720/1440/2880 = 249/35/13 spans (85/12/4%). Balanced coverage needs frameLength sampled first, then a uniform pick among valid (start,length) pairs.
   Also refresh CANONICAL.md's stale R2-conflict block and R6 parenthetical (both contradicted by the regenerated data).

9. GENERATE v3 WITH AN EXPLICIT FILENAME ALLOWLIST. `SampleAndRender <out.jsonl> <n> <seed> tempo,dynamics,articulation,rubato` — no fifth debugDir argument (it writes 3 XML files per piece). Generation is ~5 ms/piece (20k ~= 100 s). NEVER ingest as training data: pilot_v3_cov_{polyphony,stackedArtic,danglingTempo,lateStart,all}.jsonl (deliberate A4/G7/R2 violations, overlapping voices) and pilot_rubato{,_multitempo}.jsonl (Team B, 4-field rubato, R8-violating, contains NaN renders). Validate with validate_v3.py, NOT validate_data.py (the latter is tempo-only and computes ms_off = ms_at(date+dur), which articulation invalidates).

10. CPU/MEMORY DISCIPLINE. 8 GB M1, load avg 4.4, ~126 MB free pages, train.py 24 v2 v2 at 1.48 GB RSS. Everything under `nice -n 15`. Run v3 generation + preprocess AFTER the v2 run finishes if possible — preprocess.py holds every feature tensor in RAM before torch.save (train_v2.pt is 55 MB; v3 targets are ~2x longer). Do not start the v3 training while v2 is running.

ADDITIONAL NOTES: infer.py:90 calls maps_to_mpm, which now raises MpmExportError — I checked the call site and infer.py:66-67 plus dsl._sanitize_tempo already guarantee a non-empty map starting at date 0 with bpm in [1,1000], so no current path triggers it; wrap it anyway when v3 maps join. dataset.piece_to_features sorts notes by (date, pitch) while PerfChain.render requires MSM map order — identical today (0/40 pilot_v3 records have out-of-order note dates) but they diverge for chords once polyphony lands in v4. analysis/*.py insert ../python on sys.path and import dsl/evaluate/tempo_math, so steps 2 and 6 shift mdl.py's total_bits — DL in tokens is the version-invariant number to compare. Meico fork bug worth filing in bugs.md: TempoMap.java:396-404 selects the tempo segment by the unwarped GenericMap key while evaluating at the warped date.perf, so a rubato frame straddling a power transition renders NaN milliseconds (I reproduced 3/32 notes NaN through the port).