ready: False
blockers: [
 "BLOCKER B1 \u2014 VOCAB FREEZE (exact repeat of last wave's B1; must land BEFORE dsl.py is touched). `python/train.py:80` is `VOCAB_SIZES = {\"v1\": V1_VOCAB_SIZE, \"v2\": V2_VOCAB_SIZE, \"v3\": len(VOCAB)}` \u2014 a bare `len(VOCAB)` for a SHIPPED version, exactly what the comment on line 78 forbids. Verified live state: `runs/v31/ckpt.pt` = epoch 13/24, config vocab_size **31**; `runs/v3/ckpt.pt` = 31; the training process (`caffeinate -is python3 train.py 24 v31 v31`, pid 34399) is running now. The moment G/Z/Y/J are appended, `len(VOCAB)` = 35, and any RESUME of runs/v31 or runs/v3 hits the config-mismatch `raise SystemExit` at train.py:110 \u2014 the v3.1 run becomes unresumable if it ever dies (it has ~10 epochs / ~9-11 h left). Same latent bug at `python/infer.py:37` (`cfg.setdefault(\"vocab_size\", len(VOCAB) if V2 else V1_VOCAB_SIZE)`). FIX (5 lines, zero risk): add `V3_VOCAB_SIZE = 31` to dsl.py; `VOCAB_SIZES = {\"v1\": V1_VOCAB_SIZE, \"v2\": V2_VOCAB_SIZE, \"v3\": V3_VOCAB_SIZE, \"v4\": len(VOCAB)}` + a \"v4\" VERSION arm; replace infer.py:37's `len(VOCAB)` with `V3_VOCAB_SIZE`. Append-only token ids are otherwise safe (dsl.py's two decoders filter on `0 <= i < len(VOCAB)`, so old ids keep their meaning).",
 "BLOCKER B2 \u2014 TOKEN BUDGET AND TRAINING COST (the hard one; decide before generating anything). Measured on the shipped pilot_v4.jsonl under CANONICAL \u00a711's grammar: **median 768 tokens/piece, p90 1129, max 1215**, against `MAX_DECODE = 448` (train.py:55) and `max_tgt = 448` (preprocess.py:23) \u2014 53/60 pieces exceed it. Per-map medians: movement **408**, articulation **186**, dynamics 88, tempo 72, rubato 13, asynchrony 11. Even the v3-only subset is median 366 / max 589 and exceeds 448 in 12/60, purely from v4 domain randomisation (pieces to 64 beats, up to 2x tempo/dynamics segment density, articulation over the UNION of two parts' onsets). Worse, preprocess.py:42 SILENTLY skips overlong pieces (`skipped += 1`), so an unchanged pipeline would drop ~88 % of a v4 set and print only a count. Compute measured directly (d192/4+4/ff768, B=8, 2 threads, nice 15): v3.1 shape (N=51,T=171) **4.7 s fwd+bwd / 388 MB peak RSS**; full-v4 shape (N=125,T=768) **18.3 s / 1619 MB** = 3.9x time, 4.2x activation memory per sample. At the live BATCH=64 that is ~9-10 GB of activations on an 8 GB M1 (OOM/swap-thrash, the exact failure mode LOG.md iteration 0.1 documents) and ~3-5 days for 24 epochs. Mitigations measured: (N=125,T=175) = **4.2 s / 578 MB** (parity with today's v3.1 step) and (N=125,T=585) = 11.6 s / 1297 MB. CONSEQUENCE: LOG.md's v4 architecture note (\"segment maps: tempo, dynamics, rubato, **movement** via the DSL decoder\") predates the measured 408-token pedal cost and is not affordable on this machine. Either move movement to a note/frame-anchored head, or re-tune the sampler's segment-length distribution (`sampler.mjs` MOV_SEG_Q / MOV_SEG_P \u2014 Team D names this as the lever) and re-measure, or both. Articulation was already slated for a per-note head; that alone removes 186 tokens.",
 "BLOCKER B3 \u2014 ARTICULATION TARGETING IS SYSTEMATICALLY WRONG IN 2-PART DATA (supervision correctness; sampler change \u2192 regeneration, so decide first). `generate_v4.mjs:251` samples articulation dates from `distinctDates([score1, score2])` and places the map GLOBALLY (:264), but only **5.8 %** of onset dates are shared between the parts (305/5234 on the pilot). meico resolves a noteid-less `<articulation>` with `getAllElementsAt` = at-or-AFTER and adds that element unconditionally, so measured on pilot_v4.jsonl: **115/813 articulation dates are not an onset of part 1 and 650/813 (80 %) are not an onset of part 2**; validate_v4.py's own counters agree (artic_offdate=743, stacked_articulations=282, artic_unmatched=22). The DSL label says \"articulation at date d\"; the render articulates the NEXT note, and on a chord only its first note. CANONICAL A4 (\"applied per date \u2014 all notes of a chord share it\") is therefore false for v4 data, and a per-note articulation head trained on date-keyed labels would learn the wrong note. Options: (a) make articulationMap PART-LOCAL with per-part onset dates (cleanest, restores A4, sampler change); (b) keep it global and define supervision as the note INDEX PerfChainV4 computes (per-note head only \u2014 the DSL date production stops being canonical); (c) restrict dates to the intersection (too sparse at 5.8 %). Whichever is chosen must be written into CANONICAL as rule A6 AND into the sampler BEFORE the 20k run, and folded into `python/perf_chain.py` (Team A's file still matches by exact date \u2014 correct for v3, wrong for v4; PerfChainV4 only overrides it locally via `_ScoreChain`).",
 "BLOCKER B4 \u2014 GENERATOR STILL DEFAULTS TO THE DEFECTIVE RENDERER. `ml/node/generate_v4.mjs:202` is still `renderer: 'espressivo'` (its own doc line 14 says \"espressivo (default) | java\"); the java path must be requested explicitly and espressivo only WARNS on stderr (:463-467), which a piped/background 20k run will swallow. I re-confirmed E1/E2 in meico-ts source at HEAD **760d6ed (\"chore(infra): program complete\")**: `src/mpm/elements/maps/ArticulationMap.ts:103-121` fills only xml/date/xmlId/noteid/style/name.ref and never reads the numeric modifiers; `src/mpm/elements/maps/DynamicsMap.ts:100-128` parses volume/transition.to/subNoteDynamics and never reads curvature/protraction (it zeroes them only in the NO-transition branch). Blast radius on the pilot: 4053/7251 velocities, 1582/7251 ms.end. Compounding factor: per LOG.md the meico-ts program is COMPLETE and its session shut down, so nobody is on the other side \u2014 LOG.md's decision \"the v4 generator migrates to meico-ts\" is not currently executable and the Java fork must remain the labelling renderer for v4. Fix: flip the default to `java` (one line) \u2014 the warning is not a substitute for a safe default.",
 "BLOCKER B5 \u2014 NOTE-ARRAY ARITY + PART SEMANTICS BREAK THE FEATURE PATH **SILENTLY**. v4 notes are 7-element `[date,dur,pitch,msOn,msOff,vel,part]` and the JSONL is **part-major, not date-sorted** (0/60 records are globally sorted; verified). Arity itself is safe \u2014 `dataset.piece_to_features` unpacks `note[:5]` and `note[5]` \u2014 which is exactly why this fails quietly. It re-sorts by `(date, pitch)`, interleaving the parts, so with no part feature: (i) `is_chord_tone` (feature 7) fires on cross-part coincidences; (ii) `ioi_beats`/`ioi_s` and hence `local_log2_bpm` (feature 6 \u2014 the feature LOG.md's own root-cause analysis credits for v1/v2 succeeding) are computed ACROSS the part boundary, where the ms IOI carries part 2's asynchrony offset (5-60 ms, realised -58..59 on the pilot). The tempo signal is corrupted precisely at the interleave points. Required: add `part` to the feature vector and compute IOI/chord WITHIN part; take `pos_frac` from `rec[\"total_ticks\"]` when present (v4 records carry it; verified 0/60 pieces have a note ending past it) rather than from max note end. SIM2REAL GAP: `vienna_adapter.py:148` emits 6-element notes with NO part column (and both `sustain_cc` and `soft_cc`), so a part feature and the whole asynchrony head have no real-data counterpart until the adapter assigns parts \u2014 top-voice-vs-rest is exactly the split Team F measured in CANONICAL \u00a710.9 (median lead 21.5 ms, positive in 35/40 windows). The vienna-adapter agent is active in this session; coordinate BEFORE the feature layout is frozen, or the v4 model will be untestable on Vienna.",
 "BLOCKER B6 \u2014 EVAL BRANCH MUST SWITCH CHAINS AND PREPROCESS MUST CARRY THE NEW FIELDS. `evaluate.evaluate_piece_v3` builds one flat `score = [(n[0], n[1]) for n in notes]` and calls the single-part `PerfChain` \u2014 no parts, no asynchrony, no movement, and exact-date articulation matching (wrong per B3). v4 eval must route through `python/perf_chain_v4.py` (`PerfChainV4(parts=[{number, notes, <local maps>}], global_maps={...}, movement_sample_max_step=0.1).render()`; `validate_v4.py::_record_parts` is the reference adapter from a flat v4 record and handles the AS0/Y1 last-part asynchrony placement). `preprocess.py --eval` currently stores only notes/tempo/dynamics/articulation/rubato \u2014 it must also store `movement`, `asynchrony`, `total_ticks` and `sustain_cc`, and its notes tensor becomes N x 7. New metrics needed alongside render/velocity RMSE: CC RMSE plus **CC-64 threshold agreement** (findings_v4 \u00a7C recommends it precisely because CC RMSE alone under-reports pedal usefulness) and asynchrony offset error.",
 "HAZARD H1 \u2014 DSL DETAILS \u00a711 leaves open or that will bite a naive implementation. (a) The linear grammar's map ORDER for G/Y is unspecified \u2014 pin it now (suggest `... rubato* articulation* movement* asynchrony*`), because the frozen grammar cannot be reordered later. (b) `Q`/`P` are reused across the dynamics and movement productions but the implicit defaults DIFFER \u2014 verified in the fork: `DynamicsData.java:32-33` `curvature = null / protraction = null`, `MovementData.java:23-24` `curvature = 0.4 / protraction = 0.0`. The decoder must branch on production; `dsl._sanitize_dyn`'s 0.0 default must NOT be copied to movement. (c) Movement positions in the JSONL are 0..1 doubles (e.g. 0.30708661417322836 = 39/127); the encoder must emit `int(round(127*p))` per M4 \u2014 a naive `f\"{x:g}\"` emits '0.307087' (8 tokens AND off-alphabet). (d) M9 requires OMITTING curvature/protraction at 0.4/0.0, but the JSONL still records them (12 rows with curvature==0.4, 8 with protraction==0.0 on the pilot) \u2014 encoder must drop, decoder must restore. (e) The terminator is `G <date> C` with NO `Z`; the compiler must synthesise its position into the XML or `getPreviousPosition` NPEs \u2014 verified reconstructible: terminator position == value in force in **60/60** pieces. (f) Grid/alphabet conformance verified clean on the shipped pilot: 0 off-grid movement dates, 0 segments < 180 ticks, 0 off-CC-alphabet positions, 0 non-conforming terminators, 0 Y3/Y4 violations.",
 "HAZARD H2 \u2014 CC-STREAM CONVENTIONS for the pedal-state feature (last-wins, tick-0, and a real sim2real mismatch). Synthetic `sustain_cc` carries **36.6 % duplicate timestamps** (4476/12219 on the pilot \u2014 getMovementSegment emits each endpoint twice, and a plateau emits three coincident points), and it is **NOT globally monotone** (3 pieces, 3 inversions, max backstep 0.28 ms, equal values at those points) \u2014 so the extractor must SORT STABLY then apply last-wins, not scan sequentially. Real Vienna windows additionally carry **negative** ms (I measured -1.04 ms in `Chopin_op10_no3_p01_w1`) as the carried-in pedal state, plus the tick-0 burst (CANONICAL M8); synthetic always starts at exactly ms 0 with a duplicate pair. Synthetic has no `soft_cc`; Vienna has both (37 events in the record I checked). Values now agree in domain (both integer 0..127 after Team D's Math.round fix \u2014 verified). One precision detail: synthetic `sustain_cc` is part 1's UN-OFFSET stream while meico renders part 2's positionMap shifted by its asynchrony offset, so a part-2 note's true pedal state is `state(part-1 stream at note_ms - offset)` \u2014 compute pedal state in the unshifted/score domain.",
 "HAZARD H3 \u2014 ACCENTUATION: technical gate cleared, NORMATIVE gate not. E3 is fixed and verified bit-exact cross-renderer (20 pieces / 2679 notes, with a non-vacuity control showing 2679/2679 velocities move), and LOG.md line 236 records \"TD3 gate CLEARED \u2014 v4 dataset generation enables accentuation supervision (--with-accentuation on)\". But CANONICAL.md's v4 addendum explicitly says \"imprecision, **metrical accentuation** and ornamentation stay out\" (line 381), and \u00a71/H4 excludes it as an UNRESOLVED ALIAS against articulation (\"a periodic accent pattern is expressible both as accentuation and as a set of articulations\"). There are no A-rules, no band assignment, no identifiability argument for it. Keep `--with-accentuation` OFF for the v4 generation unless Team F writes the band first; otherwise the dataset contains a deliberately unidentifiable channel.",
 "HAZARD H4 \u2014 THROUGHPUT AND THE LIVE RUN. Java render ~23.7 ms/piece under load \u2192 ~8 min for 20k (Team D's re-measured figure, and their 2.8 min claim is correctly retracted); espressivo ~187 ms/piece AND wrong labels \u2192 java only. I measured `piece_to_features_v31` at **9.8 ms/piece on v4 pieces vs 2.4 ms on v3** (121 vs 51 notes) \u2192 ~3.3 min to preprocess 20k, fine. The cross-renderer gate (`verify_v4.mjs cross`) must run on a SAMPLE (<=500 pieces), never the full set. The live v3.1 run finished epoch 13 at 13:11 (3283 s/epoch, degrading to 8.8-31 s/step under contention with a vitest node process at 88 % CPU, load avg 12-14): ~10 epochs left \u2248 9-11 h, so it completes roughly 23:00-01:00. Generation, preprocessing and pilots can run alongside; the v4 TRAINING must wait for it, and B2's batch/length choice must be smoke-tested for peak RSS before committing to a multi-day run.",
 "HAZARD H5 \u2014 HYGIENE / PROVENANCE DEBT carried into the wave. (1) `ml/data/pilot_v4_espressivo.jsonl` contains knowingly-wrong velocities and note-ends (E1/E2) yet passes INVARIANTS and sits in `data/` under a name that any `pilot_v4*.jsonl` glob picks up \u2014 rename or move it out. (2) `pilot_v4_exact.jsonl` is espressivo-rendered despite its name and is a DIFFERENT configuration (no dynamics, no articulation) \u2014 not the same sample as pilot_v4.jsonl; the JSONL still records no `renderer` and no `seed` (Team E's standing request; validate_v4 now prints a map-config fingerprint as a partial substitute). (3) `ml/java/SampleAndRender.java` must NOT be deleted \u2014 `verify_v4.mjs v3proof` diffs the port against it. (4) The two NEW fork defects are NOT filed: I checked `../bugs.md` (23 lines, entries 6 and 7 only) \u2014 `MovementMap.getPreviousPosition` loops `for (j = index-1; j > 0; --j)` so instruction #1 can never inherit `position`, and the same method dereferences `getAttribute(\"transition.to\").getValue()` unguarded \u2192 NPE at `MovementMap.java:203`. Both are one-line fixes and CANONICAL M6 says fixing them refunds ~37 % of the movement DL (3350 \u2192 2094 tokens on the canonical chain) \u2014 which B2 makes load-bearing rather than cosmetic. (5) `ml/README.md` and `ml/LOG.md` still document only the Java v3 generator: no v4 generator, no flags, no `verify_v4.mjs v3proof`, no derived ULP gate, no `total_ticks`, no 6-tuple movement row, no pilots. (6) CANONICAL \u00a70's last sentence (\"the queued test \u2026 has not been run\") is stale \u2014 \u00a714 now answers it; \u00a70 was outside Team F's append-only scope."
]

ORDERED PLAN. Steps 1-4 are decisions/one-liners that must precede any code that touches dsl.py or generates data; 5-7 are the build; 8 is the gate; 9 is the run. Nothing here needs the CPU except step 8's pilot and step 9.

1. FREEZE THE VOCAB FIRST (B1), in its own commit, before a single new token is added. dsl.py: add `V3_VOCAB_SIZE = 31` next to V1/V2. train.py:80: `VOCAB_SIZES = {"v1": V1_VOCAB_SIZE, "v2": V2_VOCAB_SIZE, "v3": V3_VOCAB_SIZE, "v4": len(VOCAB)}` plus a "v4" VERSION arm and a `V4 = MODE == "v4"` flag. infer.py:37: replace the bare `len(VOCAB)`. Acceptance: `torch.load('runs/v31/ckpt.pt')['config']` still equals train.py's recomputed MODEL_CFG for MODE=v31 (vocab_size 31) AFTER the G/Z/Y/J append — i.e. re-run the resume check, do not assume it.

2. DECIDE THE ARCHITECTURE SPLIT (B2) from the measured budgets, and write the decision into LOG.md so it supersedes the pre-measurement note there. Recommended split, with the numbers behind it: DSL decoder keeps tempo+dynamics+rubato+asynchrony (median 183 tokens → T~175, probe 4.2 s / 578 MB = parity with today's v3.1 step); articulation moves to per-note encoder heads (-186 tokens, and B3 makes date-keyed articulation labels wrong anyway, so the head is the *correct* representation, not just the cheap one); pedal moves to a per-note or per-frame CC head (-408 tokens). If movement must stay in the decoder, first re-tune `sampler.mjs` MOV_SEG_Q/MOV_SEG_P and re-measure — at T=768 the full-DSL path is 18.3 s/sample-step and ~9-10 GB of activations at BATCH=64, i.e. it does not fit this machine.

3. RESOLVE ARTICULATION TARGETING (B3). Pick part-local articulationMaps (recommended: restores CANONICAL A4, keeps the DSL date production meaningful, and makes the per-note head's label unambiguous) or the note-index-label variant. Write it into CANONICAL as A6 with the meico `getAllElementsAt` = at-or-after rationale; change `sampler.mjs`/`generate_v4.mjs:251,264`; fold the same fix into `python/perf_chain.py` so no v4 consumer that calls PerfChain directly silently mis-renders. This gates generation — do not generate 20k before it lands.

4. FLIP THE RENDERER DEFAULT (B4): `generate_v4.mjs:202` → `renderer: 'java'`, keep espressivo reachable by flag and keep the ESPRESSIVO_DEFECTS block (now pinned to meico-ts HEAD 760d6ed, where E1/E2 are re-confirmed live). File E1/E2 and the two MovementMap defects in ../bugs.md while the evidence is fresh; the meico-ts program is closed, so these need a future session, not a ping.

5. EXTEND dsl.py APPEND-ONLY with `G Z Y J` per CANONICAL §11 (vocab 31 → 35, ids appended after `U F I X A L W`). Implement H1(a)-(f): pin the map order in the grammar; branch Q/P defaults on production (dynamics null→0.0 vs movement 0.4/0.0, verified in the fork); encode movement positions as `int(round(127*p))`; omit and restore M9 defaults; emit the terminator as `G <date> C` with no Z and reconstruct its position on decode. ACCEPTANCE (project standard, not a spot check): encode→decode→re-render all 60 pilot_v4 records through PerfChainV4 and require bit-exact agreement with the JSONL's own ms/velocity/CC, i.e. the same bar `validate_v4.py` already clears.

6. DATASET FEATURES (B5), coordinated with the vienna-adapter agent BEFORE the layout is frozen. Add `part` and pedal state (and optionally soft-pedal state) → N_FEATURES_V4; compute IOI / is_chord WITHIN part; take pos_frac from `rec["total_ticks"]`. Pedal state per H2: stable sort → last-wins, tolerate duplicates (36.6 %), non-monotone points (3/60 pieces, <=0.28 ms) and negative ms (real data only); for part-2 notes read the state at `note_ms - offset`. Real-data counterpart: the adapter must emit a part column (top-voice vs rest, per CANONICAL §10.9) or the part feature and the asynchrony head are untestable on Vienna.

7. v4 BRANCHES in preprocess.py / train.py / evaluate.py: new SUFFIX `_v4`, `max_tgt` from step 2, and preprocess must FAIL LOUDLY on overlong targets instead of `skipped += 1` (today it would silently drop ~88 % of a v4 set); `--eval` additionally stores movement, asynchrony, total_ticks, sustain_cc; evaluate routes through PerfChainV4 (B6) and gains CC RMSE + CC-64 threshold agreement + asynchrony offset error.

8. PILOT GATE (<=100 pieces, `--renderer java`, all under nice -n 15, no training): generate → `verify_v4.mjs invariants` + `verify_v4.mjs cross` on the sample → `validate_v4.py --cross-java` (this is the leg that covers part 2's positionMap: 0/24558 non-bit-identical over ALL parts, closing the 50 % pilot-mode CC coverage gap) → DSL round-trip from step 5 → preprocess (~9.8 ms/piece) → a 20-STEP training smoke at the chosen batch/length, recording s/step and peak RSS. Only a green smoke authorises step 9.

9. FULL RUN, sequenced against the live v3.1 job (epoch 13 done 13:11, ~3283 s/epoch, ~10 epochs left → finishes ~23:00-01:00). Generation (20k java ≈ 8 min) and preprocessing (≈3.3 min) may run alongside; the v4 TRAINING starts only after v3.1 logs TRAINING_COMPLETE, so the two never contend — the v3.1 result is also the end-to-end comparison point the hybrid split is being measured against.

STATE CONFIRMED GOOD, for the record: pilot_v4.jsonl passes INVARIANTS_PASS and validate_v4.py EXACT on disk (0/7251 notes, 0/12219 CC); movement conformance to M3/M4/M5/M9 and asynchrony conformance to Y2/Y3/Y4 are clean (0 violations of each on 60 pieces); terminators reconstruct in 60/60; the JSONL movement row is the 6-tuple with controller that python/validate_v4.py expects, so the Team D / Team E schema divergence flagged last round is resolved; sustain_cc values are integers in 0..127 in both synthetic and Vienna. The remaining divergences are the ones listed as blockers, and none of them is in the numerics.